<% content_for :title, "Αριθμός Οχημάτων - #{APP_NAME_NO_DOMAIN}" %>
<% content_for :description, "Δείτε τον αριθμό των διακριτών οχημάτων που πραγματοποίησαν τουλάχιστον μία άφιξη ανά ημέρα." %>

<div class="container mt-4">
  <h1 class="mb-4">
    <i class="bi bi-graph-up"></i> Αριθμός ενεργών οχημάτων
  </h1>

  <!-- Collapsible Info Section -->
  <div class="card border-0 shadow-sm mb-4" style="border-radius: 12px;">
    <div class="card-body p-0">
      <div class="accordion accordion-flush" id="infoAccordion">
        <div class="accordion-item border-0" style="border-radius: 8px; overflow: hidden; border: 1px solid #e9ecef !important;">
          <h2 class="accordion-header">
            <button class="accordion-button collapsed fw-semibold" type="button" data-bs-toggle="collapse" data-bs-target="#collapseInfo" aria-expanded="false" aria-controls="collapseInfo" style="background-color: #f8f9fa;">
              <i class="bi bi-chevron-right me-2 accordion-icon"></i>
              Τι φαίνεται σε αυτή τη σελίδα;
            </button>
          </h2>
          <div id="collapseInfo" class="accordion-collapse collapse" data-bs-parent="#infoAccordion">
            <div class="accordion-body bg-white p-4">
              <p class="mb-0">
                Σε αυτή τη σελίδα φαίνεται ο αριθμός των οχημάτων που πραγματοποίησαν τουλάχιστον μία άφιξη σε στάση για κάθε ημέρα. Αυτό δίνει μια εικόνα του πλήθους των οχημάτων που ήταν ενεργά την κάθε ημέρα.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <style>
    .accordion-button:not(.collapsed) .accordion-icon {
      transform: rotate(90deg);
      transition: transform 0.2s ease-in-out;
    }
    .accordion-icon {
      transition: transform 0.2s ease-in-out;
    }
    .accordion-button:not(.collapsed) {
      background-color: #e7f1ff !important;
      color: #0d6efd;
    }
    .accordion-button:focus {
      box-shadow: none;
      border-color: rgba(0,0,0,.125);
    }
  </style>

  <!-- Chart Card -->
  <div class="card border-0 shadow-sm mb-4" style="border-radius: 12px;">
    <div class="card-body p-4">

      <div class="chart-container">
        <%= line_chart(
          @results.map { |r| [r.date, r.vehicle_count] },
          colors: ["#0d6efd"],
          library: {
            backgroundColor: '#ffffff',
            borderRadius: 8
          }
        ) %>
      </div>
    </div>
  </div>
</div>
